{
  "name": "distinct-base-icons",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "root": "packages/DistinctBaseIcons",
  "targets": {
    "validate": {
      "command": "node ../../scripts/validate-mods.mjs .",
      "options": {
        "cwd": "{projectRoot}"
      }
    },
    "build": {
      "command": "dotnet build src/DistinctBaseIcons.AssetTool/DistinctBaseIcons.AssetTool.csproj --configuration Release",
      "options": {
        "cwd": "{projectRoot}"
      }
    }
  },
  "tags": ["type:mod", "format:paks"]
}
